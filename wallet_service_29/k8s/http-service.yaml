apiVersion: v1
kind: Service
metadata:
  name: http-service
  labels:
    app: wallet-service
spec:
  type: NodePort  # Use NodePort for Minikube
  ports:
  - port: 5002
    targetPort: 5002
    nodePort: 30003  # Optional: Fixed NodePort for HTTP
  selector:
    app: wallet-service