# apiVersion: v1
# kind: Service
# metadata:
#   name: wallet-service
# spec:
#   type: ClusterIP
#   ports:
#   - port: 50051
#     targetPort: 50051
#   selector:
#     app: wallet-service

apiVersion: v1
kind: Service
metadata:
  name: wallet-service
spec:
  selector:
    app: wallet-service
  ports:
    - protocol: TCP
      port: 50051
      targetPort: 50051