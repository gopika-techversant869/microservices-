apiVersion: v1
kind: Service
metadata:
  name: card-service
spec:
  selector:
    app: card
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: NodePort  # or ClusterIP/LoadBalancer as needed